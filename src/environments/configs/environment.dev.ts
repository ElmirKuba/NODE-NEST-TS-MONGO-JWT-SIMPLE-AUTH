const environment = {
  PORT: 5000,
  ACCESS_KEY_JWT: 'symbols_random_secret_key',
  REFRESH_KEY_JWT: 'other_random_secret_key',
  DB_URL: 'mongodb://localhost:27017/',
  DB_NAME: 'mongoExpressAuth12',
};

export default environment;
